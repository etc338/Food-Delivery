{
  "name": "food-delivery",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "node -e \"process.env.CI && process.exit(0); const h = require('husky'); h()\" || echo 'Skipping husky'"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
